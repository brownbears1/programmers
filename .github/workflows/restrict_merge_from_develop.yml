name: Restrict merge from develop to main


on:
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Check if the pull request is mergeable to master
      run: if [[ "$GITHUB_HEAD_REF" == 'example' ]]; then exit 0; else exit 1; fi;